# 设置默认行为，所有文件的行尾自动转换
* text=auto

# 明确声明应该被视为文本文件的文件类型
*.js text
*.vue text
*.json text
*.html text
*.css text
*.md text
*.yml text

# 声明二进制文件，不应该被修改
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.svg text

# 确保脚本文件在Unix系统上可执行
*.sh text eol=lf